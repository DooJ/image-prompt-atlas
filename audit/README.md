# 프롬프트 전수 점검 · 2026-09-18

825개 출처 사례 전체를 대상으로 본문 누락, 번역 잔여 토큰, 명시 변수와 반복 참조, 숫자·구조, JSON, 입력 이미지 안내, 중복 원문과 제목을 검사했다. 의심 사례는 한국어와 출처 원문을 대조했다. 전체 문장을 새로 번역한 작업은 아니며, 문학적 표현이나 이미지 생성 결과의 품질까지 보증하지 않는다.

- 원문 출처 기준: `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts`의 `e2a269ad1a055a0b4f6c1e170341c6c1aba30faa`.
- 해당 버전의 문서 8개와 대조하여 보관된 원문 825개가 일치함을 확인했다. 교정된 원문 편집본은 `originalTemplate`에 별도로 보관한다.
- 문장을 스타일·내용으로 자동 분해하던 편집을 전체 순서 보존 방식으로 변경했다. argument, 명시 placeholder, 선언과 참조를 연결하고 빈 입력·잘못된 JSON을 검사한다.
- JSON 32건의 한국어·원문 편집본에서 키, 객체·배열, 비문자열 값을 검증했다.
- 원문이 동일한 7쌍을 대표 사례로 표시한다. 출처 기록은 825개를 유지하고 화면에는 818개가 나타난다. 원 게시물과 예시 이미지를 보존하며 이전 주소도 대표 사례로 연결한다.
- 제목만 같은 4묶음·9개 사례는 1·2·3 번호를 붙였다. 원문과 구조를 비교한 근거는 `duplicates.json`에 있다.
- 출처 본문이 잘렸거나 후속 내용이나 실제 프롬프트가 없는 21건은 `sourceStatus: incomplete`로 표시한다. 원문을 복원할 근거가 없어 공개된 부분만 복사할 수 있다. 긴 원문이 같은 출처의 다른 문서에 있는지도 확인했으나 복원 가능한 사본을 찾지 못했다.

`review-a.json`과 `review-b.json`에는 전체 825개 점검 ID와 항목별 검토 근거가 있다. `corrections-*.json`에는 교정값, `organization.json`에는 수동 확인한 중복 통합 목록이 있다. 최종 교정값은 `data/catalog.json`이며 재적용 순서는 저장소 README를 따른다.

검증은 `npm run check`로 재현한다. 브라우저에서는 반복 변수 동시 변경, 특수문자·개행의 JSON 복사, 입력 비우기·되돌리기, 언어별 초안 보존, 불완전 출처 안내, 검색, 중복 주소 연결, 390px 화면의 가로 넘침을 확인했다.
